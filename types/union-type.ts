let userId: number | string | boolean | {name: string};
userId = 90;
userId = '88';

console.log(userId);

let customers: (number | string)[] = [];
customers.push(88);
customers.push('Juan');

console.log(customers);